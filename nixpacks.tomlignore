[phases.setup]
nixPkgs = ["python3", "perl", "gcc", "make"]
aptPkgs = ["swaks", "curl", "wget"]

[phases.install]
cmds = ["python3 -m pip install --upgrade pip", "python3 -m pip install -r requirements.txt"]

[phases.build]
cmds = []

[start]
cmd = "python3 email_sender.py"
